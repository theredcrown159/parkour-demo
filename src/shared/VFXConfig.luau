return {
	LandingDebris = {
		ChunkCountRange = NumberRange.new(2, 5),
		ChunkSizeRange = NumberRange.new(0.15, 0.35),
		OutwardSpeedRange = NumberRange.new(6, 12),
		UpwardSpeedRange = NumberRange.new(15, 25),
		AngularSpeedRange = NumberRange.new(5, 12),
		LifetimeRange = NumberRange.new(0.35, 0.65),
		FadeDuration = 0.2,
		GroundCheckDistance = 8,
		SpawnRadius = 0.5,
		SurfaceOffset = 0.05,
	},
}
